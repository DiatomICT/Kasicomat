<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.diatom.kasicomat">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/bid_box"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetaljiStednjeActivity" />
        <activity android:name=".NivoiStednjeKusurActivity" />
        <activity android:name=".NivoiStednjeFiksnoActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".PonudaActivity" />
        <activity android:name=".FakeRetailerActivity"></activity>
    </application>

</manifest>